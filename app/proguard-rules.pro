# Add project specific ProGuard rules here.
# minifyEnabled is off by default in this project (see app/build.gradle),
# so these rules only take effect if you turn R8/ProGuard on.
-keep class com.henrylumis.mediaprayer.data.** { *; }
